# Data

This folder keeps only lightweight demo data needed for simple local runs.

- `sample/` contains a few MRI images for quick testing
- Full training datasets should stay outside the repository
