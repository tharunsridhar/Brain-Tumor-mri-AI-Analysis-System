# Models

Heavy trained model files are intentionally not stored in this repository.

When you add real models later, create these folders:

- `models/Classification/`
- `models/Segmentation/`

Default model addresses are defined in `app/config.py`.
